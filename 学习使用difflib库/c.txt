from typing import List

# 输入：intervals = [[1,4],[4,5]]
# 输出：[[1,5]]
# 解释：区间 [1,4] 和 [4,5] 可被视为重叠区间。

class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        # 读取数组
        # 只需要判断前组的 尾巴 和后组的 头的关系即可
        #     如果尾巴大：
        #         重叠区间，把后组的尾巴拼过来即可
        #     如果头大：
        #         非重叠，把头为均设置成后组的
        t1 = 0
        t2 = 0