from typing import List

# 输入：intervals = [[1,4],[4,5]]
# 输出：[[1,5]]
# 解释：区间 [1,4] 和 [4,5] 可被看做重叠区间。
class Solution:
    def merge(self, intervals: List[List[int]]) -> List[List[int]]:
        # 读取数组
        # 只需要判断前组的 尾巴 和后组的 头的关系即可
        #     如果尾巴比较大：
        #         那么进行拼接
        #     如果头大：
        #         非重叠，把头为均设置成后组的
